[project]
name = "law-firm-software-hud"
version = "1.0.0"
description = "HUD environment adapter for the law_firm_software rl-env container"
requires-python = ">=3.10"
dependencies = [
    "hud>=0.6.13,<0.7",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
